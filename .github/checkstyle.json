{
  "problemMatcher": [
    {
      "owner": "checkstyle",
      "pattern": [
        {
          // This is too specific for generic use
          "regexp": "Project Directory:\\s+(\\S+)",
          "fromPath": 1
        },
        {
          // We don't need this but we want to keep matching even if there are INFO messages in the middle
          // The star matchers are for color codes
          "regexp": "^\\[.*INFO.*\\]"
        },
        {
          // The star matchers in the warning/error section are for color codes
          "regexp": "^\\[.*(WARNING|ERROR).*\\] ([^:].+):\\[(\\d+)(?:,(\\d+))?\\] \\((.+)\\) (.+)$",
          "severity": 1,
          "file": 2,
          "line": 3,
          "column": 4,
          // "code": 5,
          "message": 6,
          "loop": true
        }
      ]
    }
  ]
}
